<div class="form-container">
  <div class="form-card">
    <h1>Edit Event</h1>
    <%= simple_form_for(@event, url: profile_event_path(@profile), method: 'patch') do |f| %>
      <%= render 'form', f: f %>
    <% end %>
    <%= link_to 'Back to Event', profile_event_path(@profile, @event) %> |
    <%= link_to 'Back to Schedule', profile_events_path(@profile) %>
  </div>
</div>
